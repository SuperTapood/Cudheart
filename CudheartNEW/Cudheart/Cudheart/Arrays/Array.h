#pragma once

#include <iostream>
#include <string>

using std::string;
using std::cout;


template <typename T>
class Array {
public:
	T* arr;
	int size;
public:
	Array(T* arr, int size) {

	}
	string toString() {
		
	}
};